/*
Crea una función que reciba una frase y devuelva la palabra más larga que contenga.
*/

function palabraMasLargaSinSplit(frase) {
  let masLarga = "";
  let palabraActual = "";
  let longitudActual = 0;

  for (let i = 0; i < frase.length; i++) {
    
  }

  return masLarga;
}

// Ejemplo:
console.log(palabraMasLargaSinSplit("El zorro rápido salta sobre el perro dormido"));